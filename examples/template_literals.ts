// ── Template literals ─────────────────────────────────────────────────────────
const name = "world";
const greeting = `Hello, ${name}!`;
console.log(greeting);

const x = 7;
const y = 3;
const msg = `${x} + ${y} = ${x + y}`;
console.log(msg);

// Exit: x + y = 10
x + y
